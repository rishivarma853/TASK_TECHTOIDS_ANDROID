<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.NOTAApp"
        tools:targetApi="31">
        <activity
            android:name=".ui.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.HomeScreenActivity"
            android:exported="false" />
        <activity
            android:name=".ui.TaskScreenActivity"
            android:exported="false" />
        <activity
            android:name=".ui.AddTaskActivity"
            android:exported="false" />
        <activity
            android:name=".ui.TaskDescriptionActivity"
            android:exported="false" />
        <activity
            android:name=".LaunchScreenLoadingActivity"
            android:exported="false" />
    </application>

</manifest>
